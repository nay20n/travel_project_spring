<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:websocket="http://www.springframework.org/schema/websocket"
	xsi:schemaLocation="http://www.springframework.org/schema/websocket http://www.springframework.org/schema/websocket/spring-websocket-4.3.xsd
		http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<context:component-scan base-package="com.nh.controller" />
	
	<context:component-scan base-package="com.nh.socket" />
	
	<beans:bean id="broadSocket" class="com.nh.socket.BroadSocket"/>
	
	<websocket:handlers>
		<websocket:mapping handler="broadSocket" path="/broadcasting"/>
		<websocket:handshake-interceptors>
			<beans:bean class="org.springframework.web.socket.server.support.HttpSessionHandshakeInterceptor"/>
		</websocket:handshake-interceptors>
	</websocket:handlers>
	
	<!-- /error/ 경로로 들어오는 요청은 스프링이 가로채지 않고 톰캣이 직접 폴더를 보도록 -->
	<resources mapping="/error/**" location="/error/" />
	
	<beans:bean id="interceptor1" class="com.nh.common.AuthInterceptor"/>
	
	<interceptors>
		<interceptor>
			<mapping path="/**"/>
			<exclude-mapping path="/"/>
			<exclude-mapping path="/getBoardsLastestOrder"/>
			<exclude-mapping path="/getBoardsKeyOrder"/>
			<exclude-mapping path="/getBoardImg"/>
			<exclude-mapping path="/isExistMail"/>
			<exclude-mapping path="/sendEmail"/>
			<exclude-mapping path="/login"/>
			<exclude-mapping path="/kakaologin"/>
			<exclude-mapping path="/kakaologin/mainHome"/>
			<exclude-mapping path="/kakaologin/editInfo"/>
			<exclude-mapping path="/naverlogin"/>
			<exclude-mapping path="/naverlogin/mainHome"/>
			<exclude-mapping path="/naverlogin/editInfo"/>
			<exclude-mapping path="/join"/>
			<exclude-mapping path="/joinCode"/>
			<exclude-mapping path="/forget"/>
			<exclude-mapping path="/error/**"/>
			<exclude-mapping path="/resources/**"/>
			<beans:ref bean="interceptor1"/>
		</interceptor>
	</interceptors>
</beans:beans>
